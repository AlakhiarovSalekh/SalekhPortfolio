import { AdminShell } from "@/components/admin-shell";
import { requireRole } from "@/lib/auth/require-role";
export default async function Page(){const auth=await requireRole();return <AdminShell role={auth.role}><div className="admin-top"><div><span className="eyebrow">CONTROL CENTER</span><h1>Portfolio dashboard</h1><p>Manage projects, translations, publishing and access.</p></div></div><div className="admin-stats"><article><span>Projects</span><strong>CMS</strong><p>Add and publish without touching source code.</p></article><article><span>Languages</span><strong>EN / AZ / KA</strong><p>Independent translations with English as primary.</p></article><article><span>Access</span><strong>{auth.role??"Setup"}</strong><p>Database-level permissions with Supabase RLS.</p></article></div>{!auth.configured&&<div className="notice">Setup mode: Supabase keys are not configured, so protected database actions are disabled.</div>}</AdminShell>;}
