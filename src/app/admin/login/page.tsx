import { login } from "./actions";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="admin-auth"><form action={login} className="admin-login-card"><span className="eyebrow">PRIVATE ADMIN</span><h1>Sign in</h1><p>Only authorized Owner and Editor accounts can access this area.</p>{error==="setup"&&<div className="notice">Supabase is not configured yet. Add your project keys to .env.local.</div>}{error==="invalid"&&<div className="notice danger">Invalid email or password.</div>}{error==="unauthorized"&&<div className="notice danger">This account does not have active admin access.</div>}<label>Email<input type="email" name="email" required autoComplete="email"/></label><label>Password<input type="password" name="password" required autoComplete="current-password"/></label><button className="primary-button" type="submit">Sign in</button><a className="text-link" href="/">← Back to portfolio</a></form></main>;}
